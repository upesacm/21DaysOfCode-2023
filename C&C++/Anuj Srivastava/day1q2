#include<iostream>
using namespace std;

int main()
{
    int number , extractor = 0;

    cout<<"Enter A Number ";
    cin>>number;
    while (number>9)
    {
        extractor+=number-((number/10)*10); 
        number/=10;     
    }
    extractor+=number;
    cout<<"Sum is: "<<extractor;
}
