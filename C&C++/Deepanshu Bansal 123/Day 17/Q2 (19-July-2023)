#include<iostream>
using namespace std;

void fun(string s)
{
	int count=0;
	for(int i=0;i<s.length();i++)
	{
		if(s[i]==' ')
		{
			count++;
		}
	}
	count++;
	cout<<"Total frequency of words in sentence "<<count;
}

int main()
{
	string s;
	cout<<"Enter string s ";
	getline(cin,s);
	fun(s);
}
